{
  "_comment": "Copy to cdk.context.json and fill in your own values. cdk.context.json is gitignored.",

  "_optional_account": "Pin `account` (and optionally `region`) to make the CDK CLI refuse to deploy when the active AWS credentials belong to a different account — protects against deploying to the wrong account. Omit to follow whatever credentials are active.",
  "account": "123456789012",
  "region": "us-east-1",

  "hosted_zone_id":   "Z123ABC456DEFGHIJKLMN",
  "hosted_zone_name": "example.com",
  "custom_domain":    "ddns.example.com",

  "record_ttl": 300,

  "_optional_repo_url": "Set `repo_url` to a URL string to tag every resource with the IaC location. Omit if you don't want it.",
  "repo_url": "github.com/your-username/long-shot-ddns-aws",

  "_optional_alert_email": "Set `alert_email` to get an email when the Pi stops reporting for 30h (SNS + CloudWatch alarm). Omit to skip. You must confirm the subscription email AWS sends after deploy.",
  "alert_email": "you@example.com"
}
